1st Question: N-Queens Problem
🧩 Problem Statement

The N-Queens problem is to place N queens on an N × N chessboard such that:

No two queens attack each other.

A queen can attack:

Horizontally

Vertically

Diagonally

Return all distinct possible solutions.

Each solution should represent the board configuration.

🔎 Example

Input:

N = 4


Output:

[
 [".Q..",
  "...Q",
  "Q...",
  "..Q."],

 ["..Q.",
  "Q...",
  "...Q",
  ".Q.."]
]

🧠 Algorithm (Backtracking)

Use recursion row by row.

For each row, try placing queen in every column.

Before placing:

Check same column

Check left diagonal

Check right diagonal

If safe → place queen.

Move to next row.

If stuck → backtrack (remove queen).

⏱ Time Complexity

Worst case: O(N!)

💾 Space Complexity

O(N²) (board storage + recursion stack)

💻 Java Code
import java.util.*;

public class NQueens {

    public static List<List<String>> solveNQueens(int n) {
        List<List<String>> result = new ArrayList<>();
        char[][] board = new char[n][n];

        for (int i = 0; i < n; i++)
            Arrays.fill(board[i], '.');

        solve(0, board, result);
        return result;
    }

    private static void solve(int row, char[][] board, List<List<String>> result) {
        if (row == board.length) {
            result.add(construct(board));
            return;
        }

        for (int col = 0; col < board.length; col++) {
            if (isSafe(board, row, col)) {
                board[row][col] = 'Q';
                solve(row + 1, board, result);
                board[row][col] = '.';
            }
        }
    }

    private static boolean isSafe(char[][] board, int row, int col) {
        // Check column
        for (int i = 0; i < row; i++)
            if (board[i][col] == 'Q')
                return false;

        // Check left diagonal
        for (int i = row - 1, j = col - 1; i >= 0 && j >= 0; i--, j--)
            if (board[i][j] == 'Q')
                return false;

        // Check right diagonal
        for (int i = row - 1, j = col + 1; i >= 0 && j < board.length; i--, j++)
            if (board[i][j] == 'Q')
                return false;

        return true;
    }

    private static List<String> construct(char[][] board) {
        List<String> solution = new ArrayList<>();
        for (char[] row : board)
            solution.add(new String(row));
        return solution;
    }

    public static void main(String[] args) {
        System.out.println(solveNQueens(4));
    }
}

🔍 Line-by-Line Explanation
solveNQueens(int n)

Creates result list.

Creates board of size n×n.

Fills board with '.' (empty spaces).

Starts solving from row 0.

solve(int row, ...)

Base case:

if (row == board.length)


→ All queens placed → store solution.

Loop through every column in current row.

If safe → place queen.

Recursively solve next row.

Backtrack (remove queen).

isSafe()

Checks 3 things:

1️⃣ Column above
2️⃣ Left diagonal
3️⃣ Right diagonal

If any queen found → return false.

construct()

Converts char[][] board into required List<String> format.

📌 Step-by-Step Example (N = 4)

Place Q at (0,0)

Move to row 1

Try columns until safe spot found

Continue until row = 4

Save configuration

Backtrack and explore other possibilities
