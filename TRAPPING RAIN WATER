✅ DSA Question – Trapping Rain Water
Problem Statement

Given n non-negative integers representing an elevation map where the width of each bar is 1, compute how much water it can trap after raining.

Example Problem
Input: height = [0,1,0,2,1,0,1,3,2,1,2,1]
Output: 6
Explanation: Water trapped at each index: [0,0,1,0,1,2,1,0,0,1,0,0] → total 6 units


Another example:

Input: height = [4,2,0,3,2,5]
Output: 9

Algorithm (Two Pointer Approach)
Step-by-step

Initialize two pointers: left = 0, right = n-1

Initialize leftMax = 0, rightMax = 0, water = 0

While left < right:

If height[left] < height[right]:

If height[left] >= leftMax → leftMax = height[left]

Else → water += leftMax - height[left]

Move left++

Else:

If height[right] >= rightMax → rightMax = height[right]

Else → water += rightMax - height[right]

Move right--

Return water

Time Complexity: O(n)
Space Complexity: O(1)

Java Code
public class TrappingRainWater {

    public static int trap(int[] height) {
        int left = 0, right = height.length - 1;
        int leftMax = 0, rightMax = 0;
        int water = 0;

        while (left < right) {
            if (height[left] < height[right]) {
                if (height[left] >= leftMax) {
                    leftMax = height[left];
                } else {
                    water += leftMax - height[left];
                }
                left++;
            } else {
                if (height[right] >= rightMax) {
                    rightMax = height[right];
                } else {
                    water += rightMax - height[right];
                }
                right--;
            }
        }

        return water;
    }

    public static void main(String[] args) {
        int[] height = {0,1,0,2,1,0,1,3,2,1,2,1};
        System.out.println("Trapped Water: " + trap(height)); // Output: 6
    }
}

Code Explanation

Two pointers move from both ends

Track maximum heights from left and right

Water trapped at index = min(leftMax, rightMax) - height[i]

Sum all trapped water → final answer

Dry Run (Example)
height = [0,1,0,2,1,0,1,3,2,1,2,1]
left=0, right=11
leftMax=0, rightMax=1 → height[left]<height[right] → left++
...
Total water trapped = 6

Test Case
Input: height = [4,2,0,3,2,5]
Output: 9


✅ Why This Problem Is Important

Frequently asked at Amazon, Google, Microsoft

Tests two-pointer technique, array manipulation, and water trapping logic

Classic medium-level DSA problem
