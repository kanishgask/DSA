🧠 1) LONGEST COMMON SUBSEQUENCE (LCS)
📌 Problem Statement (Exam-ready)

Given two strings text1 and text2, find the length of the Longest Common Subsequence (LCS) between them.

A subsequence:

Maintains relative order

Characters need not be contiguous

❌ Substring → contiguous
✅ Subsequence → can skip characters

✍ Example
text1 = "abcde"
text2 = "ace"
Output = 3


Explanation:
The common subsequence is "ace", whose length is 3.

🧠 Why Dynamic Programming?

Each character comparison creates overlapping subproblems

Recursive solution is exponential

DP stores results → O(n × m) time

📐 DP Definition

Let
dp[i][j] = length of LCS between

first i characters of text1

first j characters of text2

🧮 Algorithm (Step-by-Step)

Let

n = length of text1
m = length of text2


Create a DP table:

dp[n+1][m+1]


Extra row & column represent empty string

Initialization:

If any string is empty → LCS = 0

Java auto-initializes array to 0

Fill DP table:

If characters match

dp[i][j] = 1 + dp[i-1][j-1]


Else

dp[i][j] = max(dp[i-1][j], dp[i][j-1])


Final Answer:

dp[n][m]

💻 Java Code
public class LongestCommonSubsequence {

    public static int longestCommonSubsequence(String text1, String text2) {

        int n = text1.length();
        int m = text2.length();

        int[][] dp = new int[n + 1][m + 1];

        for (int i = 1; i <= n; i++) {
            for (int j = 1; j <= m; j++) {

                if (text1.charAt(i - 1) == text2.charAt(j - 1)) {
                    dp[i][j] = 1 + dp[i - 1][j - 1];
                } else {
                    dp[i][j] = Math.max(dp[i - 1][j], dp[i][j - 1]);
                }
            }
        }

        return dp[n][m];
    }

    public static void main(String[] args) {
        String text1 = "abcde";
        String text2 = "ace";

        System.out.println(longestCommonSubsequence(text1, text2));
    }
}

🔍 Line-by-Line Explanation (VERY IMPORTANT)
int n = text1.length();
int m = text2.length();


→ Stores lengths of both strings

int[][] dp = new int[n + 1][m + 1];


→ DP table

n+1 and m+1 handle empty string cases

for (int i = 1; i <= n; i++) {
    for (int j = 1; j <= m; j++) {


→ Start from 1 because:

dp[0][*] and dp[*][0] are base cases (LCS = 0)

if (text1.charAt(i - 1) == text2.charAt(j - 1))


→ Compare characters

Index is i-1 because string is 0-indexed

dp[i][j] = 1 + dp[i - 1][j - 1];


→ Characters match

Include it in LCS

Add 1 to previous diagonal value

dp[i][j] = Math.max(dp[i - 1][j], dp[i][j - 1]);


→ Characters don’t match

Skip one character

Take maximum LCS formed so far

return dp[n][m];


→ Final cell contains LCS length of full strings

🧪 Dry Run (Core Cells)
text1 = abcde
text2 = ace

i\j	0	a	c	e
0	0	0	0	0
a	0	1	1	1
b	0	1	1	1
c	0	1	2	2
d	0	1	2	2
e	0	1	2	3

✅ Final Answer = 3

⏱ Time & Space Complexity
Type	Complexity
Time	O(n × m)
Space	O(n × m)
